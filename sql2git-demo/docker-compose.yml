services:
  postgres:
    build: .
    container_name: sql2git-demo-pg
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sql2git_demo_db
    ports:
      - "5432:5432"
    volumes:
      - ./pg_data:/var/lib/postgresql/data
      - ./dumps:/var/lib/postgresql/dumps

volumes:
  pg_data:
  dumps: